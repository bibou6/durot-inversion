import { EurosByM2Pipe } from './euros-by-m2.pipe';

describe('EurosByM2Pipe', () => {
  it('create an instance', () => {
    const pipe = new EurosByM2Pipe();
    expect(pipe).toBeTruthy();
  });
});
